export enum tags {
  user = "user",
  nodes = "nodes",
  collection = "collection",
  nodeVersions = "nodeVersions",
  privateShare = "privateShare",
}

export enum nodes {}

export enum user {
  profile = "profile",
}

export enum share {
  private = "private",
}