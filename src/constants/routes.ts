export enum site {
  app = "/app",
  web = "/web",
  terms = "/terms",
  privacy = "/privacy",
}

export enum web {
  index = "",
  login = "/login",
  magic = "/magic",
  magicExpired = "/magic/expired",
  authLink = "/auth/link",
}
export enum faq {
  index = "",
  fundamentals = "fundamentals",
  fair = "fair",
  nodes = "using-nodes",
  journals = "node-journals",
  identity = "identity",
}

export enum app {
  index = "/",
  profile = "/profile",
  updateEmail = "/updateEmail",
  nodes = "/nodes",
  admin = "/admin",
  adminAnalytics = "/admin/analytics",
  help = "/help",
}

export enum node {
  index = "/",
  objects = "/objects",
  collection = "/start",
}

export enum objects {
  index = "/",
  drive = "/drive",
  code = "/code-viewer",
}
